import 'package:flutter/material.dart';

// Color
const MaterialColor primarySwatch = Colors.green;

// Padding/Margin
const double defaultPadding = 16;
const double defaultMargin = defaultPadding / 4;

// Container
const double defaultBoxHeight = 50;
const double defaultBorderRadius = defaultMargin;

// Shadow
const List<BoxShadow> defaultShadow = [
  BoxShadow(
    blurRadius: 1,
    color: Colors.grey,
    offset: Offset(0, 2),
  )
];
